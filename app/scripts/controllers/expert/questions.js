'use strict';

/**
 * @ngdoc function
 * @name aTechClientApp.controller:ExpertQuestionsCtrl
 * @description
 * # ExpertQuestionsCtrl
 * Controller of the aTechClientApp
 */
angular.module('aTechClientApp')
    .controller('ExpertQuestionsCtrl', function ($http, $scope, apiUrl, ngNotify, $routeParams, Loading, $cookieStore) {
        console.log($routeParams.expertId);

    });
