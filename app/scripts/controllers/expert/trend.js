'use strict';

/**
 * @ngdoc function
 * @name aTechClientApp.controller:ExpertTrendCtrl
 * @description
 * # ExpertTrendCtrl
 * Controller of the aTechClientApp
 */
angular.module('aTechClientApp')
    .controller('ExpertTrendCtrl', function ($http, $scope, apiUrl, ngNotify, $routeParams, Loading, $cookieStore) {
        console.log($routeParams.expertId);

    });
